# Venue badges on the publications page.
#
# Empty on purpose: with no entry here every badge inherits the site theme
# color, so the column reads as one element.
#
# Note if you add entries back: jekyll-scholar's smallcaps/latex filters
# rewrite a straight apostrophe into a curly one, so an abbr of SC'26 renders
# as SC’26 and will not match a "SC'26" key here. Key on the curly form.
